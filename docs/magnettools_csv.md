# MagnetTools CSV Format

## Overview

MagnetTools CSV files contains Magnetic Field data in cylindrical corrdinates in $\theta=0$ frame.
$r$ and $z$ coordinates are given in m, and $B_r$, $B_z$ components are given in T.
